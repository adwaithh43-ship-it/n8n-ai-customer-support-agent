# Sanitization notes

This portfolio copy was prepared from an exported n8n workflow.

Removed or replaced:
- credential IDs/names where instance-specific
- authorization/API token values
- academy/training URLs
- instance-specific metadata
- other obvious secret-like values

Before publishing, visually inspect the JSON in a text editor and confirm that no client data or credentials remain.
